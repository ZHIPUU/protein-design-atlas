services:
  api:
    build:
      context: .
      dockerfile: backend/Dockerfile
    environment:
      ATLAS_DB: /app/data/design.db
      ATLAS_SOURCE_ROOTS: /sources
      ATLAS_ARTIFACT_DIR: /app/artifacts
    volumes:
      - ./data:/app/data
      - ./artifacts:/app/artifacts
    ports:
      - "${API_PORT:-18000}:8000"
    restart: unless-stopped

  web:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    ports:
      - "${WEB_PORT:-18082}:80"
    depends_on:
      - api
    restart: unless-stopped

  r-worker:
    build:
      context: .
      dockerfile: r/Dockerfile
    environment:
      ATLAS_DB: /app/data/design.db
      ATLAS_PLOT_DIR: /app/artifacts/plots
    volumes:
      - ./data:/app/data
      - ./artifacts:/app/artifacts
      - ./r:/app/r
    profiles: ["plots"]
